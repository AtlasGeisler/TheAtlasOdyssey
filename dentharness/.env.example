# Copy to .env and fill in. .env is gitignored and must never be committed.

# Required only when config model.provider = anthropic
ANTHROPIC_API_KEY=

# Optional: override the config file path
# DENTAL_HARNESS_CONFIG=config/config.yaml
